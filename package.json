{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "deploy": "nuxt generate && gh-pages -d dist -b main",
    "build": "nuxt build",
    "predeploy": "npm run build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@nuxt/devtools": "latest",
    "@types/node": "^18",
    "@types/three": "^0.152.1",
    "gh-pages": "^5.0.0",
    "nuxt": "^3.5.3",
    "push-dir": "^0.4.1",
    "three": "^0.153.0"
  },
  "dependencies": {
    "@vueuse/core": "^10.2.0"
  }
}
